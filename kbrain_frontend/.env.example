# KBrain Frontend Environment Variables

# API Base URL
# Default: /api (required by nginx routing)
# In development: Vite proxy forwards /api/* to localhost:8000/api/*
# In production: Nginx forwards /api/* to backend/api/*
VITE_API_URL=/api

# For direct backend access (if backend runs with /api prefix):
# VITE_API_URL=http://localhost:8000
